import { projectsMenuState as state } from "./state";
import { projectsMenuActions as actions} from "./actions";
import { projectsMenuMutations as mutations} from "./mutations";

export const projectsMenuModule = {
    state,
    actions,
    mutations
}