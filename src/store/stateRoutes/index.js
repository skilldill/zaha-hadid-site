import { stateRoutesState as state } from "./state";
import { stateRoutesActions as actions} from "./actions";
import { stateRoutesMutations as mutations} from "./mutations";

export const stateRoutesModule = {
    state,
    actions,
    mutations
}